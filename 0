{"base":"1af9b4fc79a1e5816b92292bab422287c2dc65a3","description":"feat: expand artifact location URLs in show -v\n\n- label commit and release oids in the \"Added artifact\" message\n- fix the location-add hint that passed revision positionally\n- add `expand_locations` flag to display::Release::pretty and gate\n  the per-URL rows behind it\n- pass `verbose` through from `show`, but always `false` from `list`\n\nWhy: the prior \"Added artifact <short> (<long>)\" output mixed\ncommit and release oids without labels, and the hint it printed\nused a flag form clap rejects. For the listing, expanding each\nlocation URL is useful when inspecting one release with `show -v`\nbut would make `list -v` noisy across many releases, so keep the\nscheme summary there.","oid":"2f0e6b5662870045260e52ec392f83ec980538f5","type":"revision"}