{"base":"5e8e09eb741b4f5f8c1ecf05e37eeca2a53b53cf","description":"- switch EndpointId::URL_SCHEME to the Radicle-owned `radiroh` scheme\n- reject legacy `iroh://` URLs on read (hard break; store is pre-release)\n- add is_legacy_endpoint_url so reconcile can sweep pre-rename locations\n- update tests and add regression coverage for the rejected legacy scheme\n\nThe invented `iroh://` scheme was unspecified and unowned; per rad issue\nb93d542 we mint `radiroh://` so the namespace and spec are ours.\n\n\ntest: update URL scheme in integration tests\n\n- switch locations_by_scheme fixtures and filters to `radiroh://`\n\n\nfeat(reconcile): sweep legacy iroh:// URLs under our DID\n\n- collect pre-rename `iroh://` locations alongside `radiroh://` ones\n- classify legacy URLs as stale-endpoint so --remove-orphaned-self\n  retracts them; the next reconcile re-adds a fresh `radiroh://` URL\n- cover the sweep and re-add paths with unit tests\n\nLets users clean up locations left behind by the scheme rename in a\nsingle command rather than editing the COB by hand.\n\n\ndocs: update iroh:// references to radiroh://\n\n- rename scheme in doc-comments and CLI help text across the node,\n  reconcile, seed/unseed, and location-add surfaces\n\n\ndocs: rename scheme in README and CONTEXT\n\n- update location examples to `radiroh://`\n- rename the README location-format section and document the bare-host\n  fallback\n- add an Upgrading subsection pointing at reconcile --remove-orphaned-self\n  for sweeping legacy `iroh://` URLs\n- align the seed success message with the renamed scheme\n\n\ndocs(changelog): record radiroh scheme rename and reconcile sweep\n\n- add an Unreleased entry describing the hard break and the\n  reconcile --remove-orphaned-self upgrade path\n\n\nfix(cli): reject legacy iroh:// at location add and fix fetch summary\n\n- reject `iroh://` URLs at `location add` with a message pointing at\n  `radiroh://`, instead of silently signing an unfetchable URL into the COB\n- relabel the fetch summary's URL bucket \"url\" rather than \"https\", since\n  Location::Url covers any scheme (ipfs, magnet, legacy iroh, …)\n\n\ndocs: add radiroh:// URI scheme spec\n\n- document the radiroh:// grammar, semantics, and discovery model\n- explain why discovery stays in consumer config, not the URL or COB\n- record iroh:// incompatibility and bare-host deprecation","oid":"6efd17eaa6da90e0d692a274cb8ac363613b8b63","type":"revision"}