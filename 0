{"base":"4102a987f7d3055adefa6a63b96ad699263f0ae1","description":"feat(cli): prompt to scope unseed across releases\n\n- when --release is omitted and a CID spans several releases, show a\n  picker offering each release plus an \"All releases\" entry at a TTY\n- fall back to sweeping every release for single-release CIDs and under\n  --no-input, preserving the non-interactive contract\n- thread no_input into the unseed paths and document the new behavior\n\nIntent: unseed was the one release-scoped command that silently fanned\nout to every release; the prompt makes the blast radius explicit while\nkeeping \"unseed everywhere\" reachable.","oid":"0e052702d6f19b499c609923149809172c96cfe6","type":"revision"}