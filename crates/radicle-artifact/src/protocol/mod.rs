//! Wire protocol for the rad-artifact node control socket.
//!
//! Re-exported from `radicle-artifact-core`, where the canonical
//! definitions live so protocol consumers don't need this crate.

pub use radicle_artifact_core::protocol::*;
